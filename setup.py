#!/usr/bin/env python

from distutils.core import setup

setup(name = 'OlxAPI',
    version = '1.0',
    description = '',
    author = ['ASPEN INC.'],
    author_email = [''],
    url = '',
    install_requires = [],
    package_data = {},
    packages = [
        'OlxAPI',
    ],
    package_dir = {
        'OlxAPI' : 'Python'
    }
)